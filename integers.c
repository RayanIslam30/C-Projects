#include <stdio.h>
//Code that takes in a single digit integer from user and outputs how many times that digit appears in a different integer.
int main()
{
    int num, digit, count = 0;
    printf("Enter a number: ");
    fflush(stdout);
    scanf("%d", &num);
    return 0;
}